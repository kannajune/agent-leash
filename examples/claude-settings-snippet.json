{
  "_comment": "Add to ~/.claude/settings.json (global) or <project>/.claude/settings.json (project-scoped). Replace the command path with your real one from `which agent-leash-hook` — use the ABSOLUTE path (the Claude Desktop app's hook PATH is limited). matcher 'Bash' gates shell commands only. Keep AGENT_LEASH_TIMEOUT below the 600s hook timeout.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "/Users/you/.local/bin/agent-leash-hook",
            "timeout": 600
          }
        ]
      }
    ]
  }
}
